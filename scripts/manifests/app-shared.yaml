#apiVersion: v1
#kind: Service
#metadata:
#  name: workhorse-svc
#  namespace: shared
#spec:
#  type: LoadBalancer
#  selector:
#    app: workhorse
#  ports:
#{ports}
#---
apiVersion: v1
kind: ConfigMap
metadata:
  name: workhorse-cm-init
  namespace: shared
data:
  init.sh: |
    #!/bin/bash
    sed -i "s/$(cat /etc/ssh/sshd_config | grep "^Port ")/Port ${SSH_PORT}/g" /etc/ssh/sshd_config
